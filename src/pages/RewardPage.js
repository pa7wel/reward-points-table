import {PageTitle} from "../components/PageTitle";
import {RewardTable} from "../components/RewardTable";

export const RewardPage = () => {
    return (
        <div>
            <PageTitle title={'Reward table'}/>
            <RewardTable/>
        </div>
    )
}